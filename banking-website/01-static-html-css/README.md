# 01 - Static HTML + CSS

Foundational, semantic markup and styling for the banking UI. Use this stage to learn the layout and class names before adding any scripts.

Run the pages with a Live Server extension (not `file://`). See the root `root-README.md` for paths and troubleshooting tips.
